#include "../array/array.h"
#include "../util.h"
#include "algorithms.h"

size_t partition(BarArray* arr, UpdateCallback upd) {
    size_t pivot = arr->length;
}

void quick_sort(BarArray* arr, UpdateCallback upd) {}
